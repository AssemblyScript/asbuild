{
  "name": "extends-test-build",
  "license": "MIT",
  "scripts": {
    "test": "ts-node .. && yarn test:nested && yarn test:node-resolution",
    "test:nested": "cd nested && ts-node ../..",
    "test:node-resolution": "cd node-resolution && ts-node ../.."
  }
}
